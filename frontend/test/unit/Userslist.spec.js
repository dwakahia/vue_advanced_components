import Vue from 'vue'
import Users from "../../components/users/Users";
import {shallowMount} from "@vue/test-utils";

describe('show user list when no users', () => {
    it('check if datatable is rendered', () => {
        const constructor = Users({

        })
    })
});